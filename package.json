{
  "name": "@mgmorris/website-legos",
  "version": "1.0.4",
  "description": "Building blocks for a quick website",
  "main": "dist/index.js",
  "repository": "mgmorris/website-legos",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "rm -rf ./dist && npm run build",
    "prepublishOnly": "babel ./src --out-dir ./dist -s inline"
  },
  "author": "mgmorris",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.6.0",
    "@babel/core": "^7.6.0",
    "@babel/preset-env": "^7.6.0",
    "@babel/preset-react": "^7.0.0",
    "react": "^16.9.0"
  },
  "peerDependencies": {
    "react": "^16.9.0"
  },
  "keywords": [
    "react",
    "website-building",
    "quick-sites"
  ],
  "files": [
    "dist/*"
  ]
}
